ifndef::imagesdir[]
:imagesdir: ../images
endif::imagesdir[]

[[section-context-and-scope]]
== Context and Scope

This section defines the system boundary of Yovi.
It shows who interacts with the platform, which technical channels are used, and how the main services collaborate at runtime inside the Docker Compose environment.

=== Business Context

[plantuml, target=business-context-diagram, format=png]
----
@startuml
title Business Context Diagram

actor Player
actor Bot

package "Docker Compose Environment" {
    node "Gateway (nginx)" as Gateway
    node "Webapp\n(React + Vite + TypeScript)" as WebApp
    node "Auth Service\n(Node.js + Express)\nport: 3500" as Auth
    node "Stats Service\n(Node.js + Express)\nport: 3001" as Stats
    node "Gamey Service\n(Rust engine + bot service)\nport: 4000" as Gamey
    database "User Authentication DB\n(MongoDB)" as UserDB
    database "Game Statistics DB\n(MongoDB)" as GameDB
}

Player --> Gateway : Open web application\nplay games\nview history and stats
Gateway --> WebApp : Serve SPA
Gateway --> Auth : Authentication flow
Gateway --> Gamey : Game and matchmaking requests
Gateway --> Stats : Player stats and history requests

Bot --> Gamey : Request gameplay actions
Bot --> Stats : Read statistics

Auth --> UserDB : Store users and password hashes
Gamey --> Stats : Report finished matches
Stats --> GameDB : Persist history and aggregates
@enduml
----

=== Business Context Elements

* `Player`: main human user of the platform through a browser.
* `Bot`: automated consumer of the gameplay and stats APIs.
* `Gateway`: single public entry point of the system.
* `Webapp`: SPA that renders login, dashboard, gameplay, history and help.
* `Auth Service`: registration, login, token verification and account persistence.
* `Gamey Service`: game rules, bot execution, online matchmaking and active session state.
* `Stats Service`: match history and player statistics persistence/query layer.
* `User Authentication DB`: MongoDB database used only by `auth_service`.
* `Game Statistics DB`: MongoDB database used only by `stats`.

=== Technical Context

[plantuml, target=technical-context-diagram, format=png]
----
@startuml
title Technical Context - Deployment and Interfaces

actor "Player (Browser)" as Browser
actor "External Bot" as ExternalBot

package "Docker Compose Environment" {
    node "Gateway\n(nginx)\nhost ports: 8080 / 443" as Gateway
    node "Webapp\n(React + Vite + TypeScript)\ninternal port: 80" as WebApp
    node "Auth\n(Node.js + Express)\nport: 3500" as Auth
    node "Stats\n(Node.js + Express)\nport: 3001" as Stats
    node "Gamey\n(Rust engine + bot service)\nport: 4000" as Gamey
    database "Mongo Auth\nport: 27017" as UserDB
    database "Mongo Stats\nhost port: 28018\ncontainer port: 27017" as GameDB
    node "Prometheus\nport: 9090" as Prometheus
    node "Grafana\nhost port: 9091" as Grafana
}

Browser --> Gateway : HTTP/HTTPS
ExternalBot --> Gamey : HTTP JSON
ExternalBot --> Stats : HTTP JSON

Gateway --> WebApp : HTTP
Gateway --> Auth : HTTP JSON
Gateway --> Stats : HTTP JSON
Gateway --> Gamey : HTTP JSON

Auth --> UserDB : Mongo driver
Stats --> GameDB : Mongo driver
Gamey --> Stats : POST /internal/v1/matches/finished

Gateway --> Prometheus : /metrics
Auth --> Prometheus : /metrics
Stats --> Prometheus : /metrics
Gamey --> Prometheus : /metrics
Prometheus --> Grafana : Prometheus data source
@enduml
----

=== Technical Interfaces

[options="header"]
|===
| Participant | Input / Output | Channel / Protocol

| `Player (Browser)`
| Open the SPA, authenticate, create games, play turns and inspect history.
| HTTP/HTTPS through the gateway

| `External Bot`
| Query gameplay and statistics APIs outside the browser flow.
| HTTP JSON

| `Gateway`
| Public entry point that serves `/`, `/auth/*`, `/api/*`, `/stats/*` and `/external/*`.
| HTTP/HTTPS externally, HTTP internally

| `Webapp`
| Render the SPA and call backend APIs through the gateway.
| HTTP inside Docker

| `Auth Service`
| Registration, login, JWT verification and user persistence.
| HTTP REST plus MongoDB driver

| `Gamey Service`
| Game creation, bot execution, matchmaking, online turns and internal reporting to stats.
| HTTP REST

| `Stats Service`
| Player aggregates, match history and internal finished-match ingestion.
| HTTP REST plus MongoDB driver

| `Databases`
| Persistent storage for auth identities and statistics/history.
| MongoDB wire protocol

| `Prometheus`
| Scrape `/metrics` from runtime services.
| HTTP

| `Grafana`
| Visualize dashboards over Prometheus data.
| HTTP
|===

==== Observability Context

The technical context also includes an embedded observability stack for local execution and demos.
`Prometheus` is available at `http://localhost:9090` and scrapes `gateway`, `auth`, `stats` and `gamey` every `15s` through their `/metrics` endpoints, as configured in `monitoring/prometheus/prometheus.yml`.
`Grafana` is available at `http://localhost:9091`, uses that Prometheus instance as its default data source and provisions the dashboard `Yovi Observability`.

This monitoring path matters architecturally because it gives direct visibility into both cross-service HTTP behavior and game-specific runtime data such as matchmaking queue size, active games and finished games still kept in memory by `gamey`.
