ifndef::imagesdir[:imagesdir: ../images]

[[section-building-block-view]]

== Building Block View

ifdef::arc42help[]
[role="arc42help"]
****
.Content
The building block view shows the static decomposition of the system into building blocks (modules, components, subsystems, classes, interfaces, packages, libraries, frameworks, layers, partitions, tiers, functions, macros, operations, data structures, ...) as well as their dependencies (relationships, associations, ...)

This view is mandatory for every architecture documentation.
In analogy to a house this is the _floor plan_.

.Motivation
Maintain an overview of your source code by making its structure understandable through
abstraction.

This allows you to communicate with your stakeholder on an abstract level without disclosing implementation details.

.Form
The building block view is a hierarchical collection of black boxes and white boxes.

*Level 1* is the white box description of the overall system together with black
box descriptions of all contained building blocks.

*Level 2* zooms into some building blocks of level 1.
Thus it contains the white box description of selected building blocks of level 1, together with black box descriptions of their internal building blocks.

*Level 3* zooms into selected building blocks of level 2, and so on.


.Further Information

See https://docs.arc42.org/section-5/[Building Block View] in the arc42 documentation.

****
endif::arc42help[]

This section describes the static decomposition of Yovi in three levels. The first level presents the overall system. The second level zooms into the blocks that are most relevant for understanding the architecture. The third level refines the most complex internal coordination logic.

=== Whitebox Overall System

[plantuml,"05_level1_yovi_overall",png]
----
@startuml
title Level 1 - Whitebox Overall System
left to right direction
skinparam componentStyle rectangle

actor Player

rectangle "Yovi" {
  component "Webapp" as Webapp
  component "Gateway" as Gateway
  component "Auth Service" as Auth
  component "Gamey Service" as Gamey
  component "Stats Service" as Stats
  database "Mongo Auth" as MongoAuth
  database "Mongo Stats" as MongoStats
}

Player --> Gateway : HTTP
Gateway --> Webapp : /
Gateway --> Auth : /auth/*
Gateway --> Gamey : /api/*
Gateway --> Stats : /stats/*
Gamey --> Stats : /internal/v1/matches/finished
Auth --> MongoAuth
Stats --> MongoStats
@enduml
----

Motivation::
+
The overall system is decomposed into a browser frontend, a single public entry point, three domain services and two persistence nodes. This separation makes the main responsibilities visible without exposing implementation details too early.

Contained Building Blocks::
+
[cols="1,3,2,2",options="header"]
|===
|Name | Responsibility | Interfaces | Code

|`Webapp`
|Single-page application for login, game configuration, gameplay, match history and help views.
|Uses `/auth/*`, `/api/*` and `/stats/*` through the gateway.
|`webapp/`

|`Gateway`
|Single external entry point, proxy router and public API facade.
|Exposes `/`, `/auth/*`, `/api/*`, `/stats/*`, `/external/*`.
|`gateway/gateway-service.js`

|`Auth Service`
|Registration, login, token verification and account persistence.
|`/register`, `/login`, `/verify`
|`auth_service/`

|`Gamey Service`
|Game rules, bot execution, active sessions, matchmaking and timeout supervision.
|`/v1/games/*`, `/v1/matchmaking/*`, `/v1/ybot/choose/*`
|`gamey/`

|`Stats Service`
|Persistence and query API for match history and player statistics.
|`/v1/me`, `/v1/me/history`, `/internal/v1/matches/finished`
|`stats/`

|`Mongo Auth`
|Persistent storage of users and password hashes.
|MongoDB connection from `auth_service`
|Docker volume `mongo-auth-data`

|`Mongo Stats`
|Persistent storage of finished matches and aggregate counters.
|MongoDB connection from `stats`
|Docker volume `mongo-stats-data`
|===

Important Interfaces::
+
* `Player -> Gateway`: all browser traffic enters the system through one public HTTP endpoint.
* `Gateway -> internal services`: the gateway strips prefixes and forwards requests to the owning service.
* `Gamey Service -> Stats Service`: finished matches are reported internally without involving the browser.

=== Level 2

Level 2 zooms into the blocks with the highest architectural relevance: `Webapp`, `Gateway` and `Gamey Service`. `Auth Service` and `Stats Service` remain at black box level because their internal structure is comparatively simple.

==== White Box `Webapp`

[plantuml,"05_level2_webapp",png]
----
@startuml
title Level 2 - White Box Webapp
left to right direction
skinparam componentStyle rectangle

component "App shell" as App
component "Views" as Views
component "useAuth" as UseAuth
component "useGamey" as UseGamey
component "useStats" as UseStats
component "API clients" as Api
database "Session store" as Store

App --> Views
App --> UseAuth
App --> UseGamey
App --> UseStats
UseAuth --> Api
UseGamey --> Api
UseStats --> Api
UseAuth --> Store
UseGamey --> Store
@enduml
----

Motivation::
+
The frontend is organized around a small application shell, presentation views and dedicated hooks for the main user flows. This keeps rendering, remote communication and local persistence clearly separated.

Contained Building Blocks::
+
[cols="1,3,2,2",options="header"]
|===
|Name | Responsibility | Interfaces | Code

|`App shell`
|Selects the active screen and connects hooks with views.
|Internal React composition.
|`webapp/src/App.tsx`

|`Views`
|Render login, dashboard, game, history, help and supporting UI components.
|React props from the app shell and hooks.
|`webapp/src/views/`

|`useAuth`
|Manages authentication state, guest mode, token verification and logout.
|Calls auth endpoints through API helpers and browser storage.
|`webapp/src/hooks/useAuth.ts`

|`useGamey`
|Handles game creation, matchmaking, online synchronization and in-game actions.
|Calls game endpoints and persists resumable game data.
|`webapp/src/useGamey.ts`

|`useStats`
|Loads player aggregates and recent match history.
|Calls stats endpoints for authenticated users.
|`webapp/src/useStats.ts`

|`API clients`
|Wrap low-level HTTP interaction with backend services.
|`fetch` calls to `/auth`, `/api` and `/stats`.
|`webapp/src/gameyApi.ts`, `webapp/src/statsApi.ts`

|`Session store`
|Persists active session and resumable game metadata.
|Browser local storage.
|`webapp/src/gameSessionStore.ts`
|===

Important Interfaces::
+
* `useAuth -> /auth/*`: login, registration and token verification.
* `useGamey -> /api/*`: game lifecycle, matchmaking and bot hints.
* `useStats -> /stats/*`: player statistics and match history.

==== White Box `Gateway`

[plantuml,"05_level2_gateway",png]
----
@startuml
title Level 2 - White Box Gateway
left to right direction
skinparam componentStyle rectangle

component "Proxy route map" as Proxy
component "External API router" as ExternalApi
component "Error normalization" as Errors
component "Health endpoint" as Health

component "Webapp" as Webapp
component "Auth Service" as Auth
component "Gamey Service" as Gamey
component "Stats Service" as Stats

Proxy --> Webapp
Proxy --> Auth
Proxy --> Gamey
Proxy --> Stats
ExternalApi --> Auth
ExternalApi --> Gamey
ExternalApi --> Stats
Errors ..> Proxy
Errors ..> ExternalApi
Health ..> Proxy
@enduml
----

Motivation::
+
The gateway is intentionally thin. It centralizes public routing and normalized failure handling, while keeping business rules in the backend services that own them.

Contained Building Blocks::
+
[cols="1,3,2,2",options="header"]
|===
|Name | Responsibility | Interfaces | Code

|`Proxy route map`
|Maps public prefixes to internal services.
|`/`, `/auth/*`, `/api/*`, `/stats/*`
|`gateway/gateway-service.js`

|`External API router`
|Exposes public endpoints for external consumers and bot-related documentation.
|`/external/*`
|`gateway/gateway-service.js`

|`Error normalization`
|Transforms upstream connection failures into predictable HTTP `502` responses.
|JSON error response to the browser.
|`gateway/gateway-service.js`

|`Health endpoint`
|Provides a basic liveness check.
|`/health`
|`gateway/gateway-service.js`
|===

Important Interfaces::
+
* `Gateway -> Webapp`: forwards root traffic to the SPA.
* `Gateway -> Auth Service`, `Gamey Service`, `Stats Service`: forwards route-specific API traffic.
* `Gateway -> browser`: returns normalized `502 Bad Gateway` on upstream failures.

==== White Box `Gamey Service`

[plantuml,"05_level2_gamey",png]
----
@startuml
title Level 2 - White Box Gamey Service
left to right direction
skinparam componentStyle rectangle

component "HTTP API" as Api
component "Game session store" as Sessions
component "Matchmaking state" as Matchmaking
component "Matchmaking worker" as Worker
component "Timeout monitor" as Monitor
component "GameY engine" as Engine
component "Bot registry" as Bots
component "Stats reporter" as Reporter
component "Stats Service" as Stats

Api --> Sessions
Api --> Matchmaking
Api --> Engine
Api --> Bots
Api --> Reporter
Worker --> Matchmaking
Worker --> Sessions
Worker --> Engine
Monitor --> Sessions
Reporter --> Stats
@enduml
----

Motivation::
+
`Gamey Service` is the domain core of the system. It concentrates the rules of the game, online coordination and bot logic so that gameplay remains consistent and isolated from the rest of the platform.

Contained Building Blocks::
+
[cols="1,3,2,2",options="header"]
|===
|Name | Responsibility | Interfaces | Code

|`HTTP API`
|Exposes endpoints for games, moves, resign/pass actions, matchmaking and bots.
|HTTP/JSON endpoints under `/v1/*`
|`gamey/src/bot_server/mod.rs`

|`Game session store`
|Keeps active matches and related metadata in memory.
|Shared internal state.
|`gamey/src/bot_server/state.rs`

|`Matchmaking state`
|Stores waiting tickets and matched ticket information in memory.
|Shared internal state.
|`gamey/src/bot_server/state.rs`

|`Matchmaking worker`
|Pairs waiting players and creates online games with player tokens.
|Background worker over shared matchmaking state.
|`gamey/src/bot_server/matchmaking.rs`

|`Timeout monitor`
|Auto-passes turns and finishes inactive online games.
|Background worker over active sessions.
|`gamey/src/bot_server/games.rs`

|`GameY engine`
|Validates moves, applies actions and determines winners.
|Internal domain API.
|`gamey/src/core/`

|`Bot registry`
|Resolves the configured bot and computes automated moves.
|Internal bot selection and execution.
|`gamey/src/bot/`

|`Stats reporter`
|Sends finished match summaries to the stats service.
|HTTP call to `/internal/v1/matches/finished`
|`gamey/src/bot_server/games.rs`
|===

Important Interfaces::
+
* `x-user-id`: identifies authenticated users when a game is created or queried.
* `x-opponent-user-id`: supports direct online game creation.
* `x-player-token`: protects turn ownership in matchmaking-created online games.

=== Level 3

Level 3 opens the `Gamey Service` block introduced in level 2. The refinement focuses on its online gameplay runtime, because that slice contains the most coordination-heavy behavior: request handling, shared in-memory state, background supervision and completion reporting.

Bot execution remains at level 2 because it is orthogonal to the online human-vs-human flow documented here.

==== White Box Gamey Service - Online Gameplay Runtime

[plantuml,"05_level3_online_coordination",png]
----
@startuml
title Level 3 - White Box Gamey Service: Online Gameplay Runtime
left to right direction
skinparam componentStyle rectangle

component "Game API" as Games
component "Matchmaking API" as MatchApi
component "Runtime state" as Runtime
component "Background coordinators" as Workers
component "GameY engine" as Engine
component "Completion reporting" as Reporter
component "Stats Service" as Stats

Games --> Runtime
Games --> Engine
MatchApi --> Runtime
Workers --> Runtime
Workers --> Engine
Games --> Reporter
Workers --> Reporter
Reporter --> Stats
@enduml
----

Motivation::
+
This white box zooms into the online human-vs-human runtime of `Gamey Service`. It mixes synchronous HTTP requests with asynchronous workers and must keep online matches consistent even though the runtime state is stored in memory.

Contained Building Blocks::
+
[cols="1,3,2,2",options="header"]
|===
|Name | Responsibility | Interfaces | Code

|`Game API`
|Serve game creation, querying and in-game actions for active matches.
|HTTP handlers for `/v1/games/*`
|`gamey/src/bot_server/games.rs`

|`Matchmaking API`
|Serve enqueue, ticket query and cancellation operations for waiting players.
|HTTP handlers for `/v1/matchmaking/*`
|`gamey/src/bot_server/matchmaking.rs`

|`Runtime state`
|Keep active sessions, active-game ownership and matchmaking data in shared memory.
|Internal state shared through `AppState`.
|`gamey/src/bot_server/state.rs`

|`Background coordinators`
|Run asynchronous tasks that pair players and enforce inactivity and turn-timeout rules.
|Internal scheduled workers.
|`gamey/src/bot_server/matchmaking.rs`, `gamey/src/bot_server/games.rs`

|`GameY engine`
|Validate moves and actions, evolve the board and determine winners.
|Internal domain API.
|`gamey/src/core/`

|`Completion reporting`
|Send finished-match summaries to the statistics service after game completion.
|HTTP call to `/internal/v1/matches/finished`
|`gamey/src/bot_server/games.rs`
|===

Important Interfaces::
+
* `Game API -> Runtime state`: load sessions, update player presence and persist match progress.
* `Matchmaking API -> Runtime state`: create waiting tickets, expose queue status and publish matched results.
* `Background coordinators -> Runtime state`: consume waiting entries and enforce online timeout rules.
* `Game API / Background coordinators -> GameY engine`: apply moves and actions using the same rule implementation.
* `Completion reporting -> Stats Service`: persist the finished match outcome through the internal stats endpoint.
